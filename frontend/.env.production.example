# Frontend (React) production environment variables
# Copy to frontend/.env.production and set your production API URL (served by Nginx reverse proxy)
/*
  In production, the frontend must call the backend through Nginx at:
    https://YOUR_DOMAIN_OR_IP/api
  This matches the Nginx location /api/ -> proxy_pass http://127.0.0.1:3001/api/
*/
REACT_APP_API_URL=https://YOUR_DOMAIN_OR_IP/api

# Optional helpers (only if your build setup reads them)
# REACT_APP_ENV=production
# PUBLIC_URL=https://YOUR_DOMAIN_OR_IP
